# 🔄 작업 인수인계 (노트북 → 데스크탑)

> 마지막 갱신: 2026-06-13 (폰 데모 2차 피드백 반영 세션)
> **데스크탑에서 재개 시: `git pull` 후 이 문서부터 읽으세요.**

---

## 1. 현재 상태

- **브랜치**: `main` (모든 작업 직접 커밋, origin/main과 동기화 완료 — 머지 대기 브랜치 없음)
- **빌드**: 타입체크 통과(`tsc --noEmit` 에러 0), 헤드리스 시뮬 정상
- **EAS 빌드 설정 완료**: `eas.json` + `app.json`(package `com.kkustudio.toweroffense`, projectId 연결)
  - 클라우드 빌드: `npx eas build -p android --profile preview` → APK 다운로드 링크
- `git pull` 하면 아래 "이번 세션 완료" 내용이 전부 들어옵니다.

---

## 2. 이번 세션 완료 (폰 2차 피드백)

| # | 내용 | 커밋 |
|---|---|---|
| 1 | 유닛 "모여서 출발" 제거 → 물량 스트림 (랠리 대기 off, 스폰 BASE 2.0/카드 1.0) | 1d60a43 |
| 2 | 영웅 성 공성 + 체젠 1%/s + 부활 90→30초 | 1d60a43 |
| 8 | 영웅 탱킹 배수 x3→**x2** (마루한 320/70, 미르 240/30, 노을 260/30) | 4234e33 |
| 4 | 적 유닛 배회 (성에만 안 붙고 타워 주변 [16,52] 순찰) | a8ea990 |
| 5 | 아군 = 성 포위 360° (화면 가장자리에서 생성→사방 공성) | a8ea990 |
| — | 동시생존 캡 양측 동일화 (110→**150 대 150**) | 1eb22f9 |
| — | EAS 빌드 설정 | 33d29ea |

**답변만 한 항목** (코드 변경 없음):
- **7. 영웅 스탯 찍기** = 이미 구현됨(영웅 탭). 스탯 포인트는 영웅 메타 레벨업(스테이지 클리어/중복뽑기)으로 획득 — 포인트 0이면 버튼 비활성
- **9. 시간당 생성**: 적 = spawnRate 0.5~5.0/s(캡 150), 아군 = (2.0+1.0×유닛카드수)×생성속도(캡 150)

---

## 3. ▶ 다음 작업 큐 (이 순서로)

### ~~6. 전투 영웅 선택~~ ✅ 완료 (2026-06-13)
- `progressStore.selectedHeroId`(영구 저장) + `selectHero()`, 영웅 탭 출전 버튼, 홈 출전 영웅 바
- `startStage`·`BattleField`가 선택 영웅 + 메타 스탯·skillLevel 반영 (메타 스탯 미반영 버그도 같이 해결)

### ~~3. 카드 개편 + 근접 유지력~~ ✅ 완료 (2026-06-13)
> Step A: 유닛 카드 Lv1~5 트랙 재구조화 + 5종 특수기술(수호오라·멀티샷 포함) + CardPickModal UI
> Step B: item 5 해금 7종 유닛 전투 카드 풀 연동(스탯업 트랙) + 근접병(방패/검사·적 근접) 유지력↑
**유저 승인 완료한 구조 — 그대로 구현:**
- 유닛 카드 = **Lv1→Lv5 업그레이드 트랙** (특성 5장을 유닛 레벨에 흡수)
  - Lv1 생성 / Lv2 스탯업 / **Lv3 특수기술#1** / Lv4 스탯업 / **Lv5(MAX) 특수기술#2**
- 특수기술 매핑 (확정):
  | 유닛 | Lv3 | Lv5 |
  |---|---|---|
  | 방패병 | 도발 | 수호 오라(주변 아군 피해감소) |
  | 활병 | 불화살 | 멀티샷(2발 동시) |
  | 창병 | 돌진 | 출혈 |
  | 투석기 | 화염공격 | 기절 |
  | 검사 | 흡혈 | 버서커 |
- 글로벌 17장은 그대로 유지. 카드 풀 47장 → **22트랙**(유닛 5 + 글로벌 17)
- **추가: 근접병 유지력↑** (방패병/검사 + 적 근접 유닛 공통 — HP/방어/체젠 상향)
- 영향 파일: `data/cards.ts`(재구조화), `engine/cards.ts`(풀 로직 + 유닛 레벨별 mods), `components/CardPickModal.tsx`, 메타 유닛강화와의 상호작용

### ~~2. 상점 개편 + 일일선물~~ ✅ 완료 (2026-06-13)
- 골드 구매=다이아로(`GOLD_SHOP_PACKS`/`buyGoldPack`) · 묶음 소환 x1/x10/x100(할인, `bulkGachaCost`/`GACHA_TIERS`)
- 다이아 현금팩 x1/x10/x100(보너스↑, IAP `diamonds_x*`) · 일일 무료 선물(`claimDailyGift`/`dailyGiftDate`)
- `shop.tsx` 전면 재구성 (일일선물→소환→골드상점→다이아충전→무료다이아→광고제거)

> **✅ HANDOFF 다음 작업 큐(6·3·2) 전부 완료.**
> 추가 완료: 프리미엄 패스(광고제거+x4+리롤무제한+골드2배, 가격 결정 대기) / ~~신규 7종 유닛 특수기술~~ ✅(2026-06-14)
> 후속 후보: 폰 테스트 후 밸런스(투석기 등) / 사운드·연출 / 프리미엄 패스 가격 확정

---

## 4. ⚠️ 밸런스 후속 (폰 테스트 결과 반영 예정)

- **클리어가 너무 빠름** — 영웅 공성+물량 러시로 타워 순삭(시뮬 스1 ~25초, 스10 ~1분40초). 10분 세션 대비 짧음
  - 조정 후보: 타워 방어력↑ / 성벽 HP↑ / 영웅이 타워 직행 전 주둔군 우선 정리
- **150v150 성능** — 폰에서 프레임 끊기면 `ENEMY_MAX_ALIVE`/`ALLY_MAX_ALIVE`를 100으로 (engine.ts 상단 상수)

---

## 5. 빌드 / 테스트

- **EAS 클라우드** (계정 연결됨): `npx eas build -p android --profile preview` → APK 링크
- **로컬 (노트북 전용 메모)**: JDK 21 = `C:\1\jbr` (Android Studio 번들), `JAVA_HOME`/`ANDROID_HOME` 영구 등록됨. USB 연결 후 `npx expo run:android`
  - 데스크탑은 기존 빌드 환경 그대로 사용
- 헤드리스 밸런스 시뮬: `npx tsx scripts/simBattle.ts [스테이지]`

---

## 6. 참고

- 전체 설계: `docs/design/00~16` + 결정 로그는 `PROGRESS.md`
- 게임 개요: 루트 `README.md`
